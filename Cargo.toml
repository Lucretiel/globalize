[package]
name = "globalize"
version = "1.0.0"
edition = "2024"

readme = "./README.md"
license = "MPL-2.0"
description = "A type-safe way to interact with removing from and inserting into options."
repository = "https://github.com/Lucretiel/globalize"

[dependencies]
quote = { version = "1.0", default-features = false }
syn = { version = "3.0", default-features = false, features = [
    "full",
    "parsing",
    "printing",
    "proc-macro",
] }

[lib]
proc-macro = true
